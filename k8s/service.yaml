apiVersion: v1
kind: Service
metadata:
  name: flask-service
spec:
  type: NodePort
  selector:
    app: flask-devops   # ✅ نفس اللي موجود في Pod
  ports:
    - port: 80
      targetPort: 5000
      nodePort: 30007   # NodePort ثابت